# Direkter Zugriff auf Daten und Bilder ist gesperrt.
# Bilder werden nur über bild.php ausgeliefert (prüft die Freischaltung).
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
